top_level_struct:
  paths: ["sub1/*.txt", "sub2/*.txt"]
  filters:
    quirk: last_match
    diff_regex: ".*"
  actions:
    comments: ["Hello from the top level"]
    reviewers: ["top-reviewer"]
